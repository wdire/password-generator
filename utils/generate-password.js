const generatePassword = (length, options) => {
  console.log('options:', options, 'length:', length);

  // Password generation

  return 'generated-pass-' + length;
};

export default generatePassword;
